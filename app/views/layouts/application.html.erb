<!DOCTYPE html>
<html>
  <head>
    <title>ShadcnRailsTemplate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header><%= render 'layouts/navbar' %> <%= render 'layouts/alerts' %></header>
    <main>
      <blockquote class="text-xl italic font-semibold text-left text-gray-900 dark:text-white p-4 mt-28"><%= yield %></blockquote>
    </main>
  </body>
</html>
