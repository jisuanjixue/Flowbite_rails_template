<h1 class="text-center"><%= pluralize(@posts.count, "Result") %></h1>
<div class="container">
  <table class="table table-dark table-striped">
    <thead>
      <tr>
        <th><%= sort_link(@query, :title, "Title", default_order: :asc) %></th>
        <th><%= sort_link(@query, :category_name, "Category") %></th>
        <th><%= sort_link(@query, :description, "Blog Content") %></th>
        <th><%= sort_link(@query, :user_username, "Users") %></th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= link_to post.title, post %></td>
          <td><%= link_to post.category.name, post.category %></td>
          <td><%= post.description.to_plain_text.truncate_words(25) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
